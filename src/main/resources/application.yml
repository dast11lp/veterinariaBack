spring:
      datasource:
                url: jdbc:mysql://localhost/veterinaria4
                username: root
                password: 
                driver-class-name: com.mysql.cj.jdbc.Driver
      jpa:
          database-platform: org.hibernate.dialect.MySQL8Dialect
logging:
        level:
              org:
                  hibernate:
                            SQL: debug

